import 'package:flutter/material.dart';

const primaryColor = Color(0xff0575E6);
const secondaryColor = Color(0xff2948ff);
const gradientColor = LinearGradient(
  begin: Alignment.topCenter,
  end:Alignment.bottomCenter,
  colors: [
    primaryColor,
    secondaryColor,
  ]
);

